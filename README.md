# algorithms-visualization
An application that can visualize the data structure and algorithm used in coding for better learning
1. Allow user to interact with data structure such as queue, stack etc with action like push, pop, ...
2. Allow user to test out the algorithm with the test set and input then show the result
3. Interactive UI/UX